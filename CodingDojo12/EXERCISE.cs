namespace ConsoleApplication1
{
    /*
        Exercise:
        Create a static class with a generic method that accomplish the following criteria:
        Receives a type that inherits from an interface.
        Creates the object and executes a method that prints something to the console.
        If the method is called with the same type, a log should say that it was already called.
     */
    
    public class EXERCISE
    {
        
    }
}